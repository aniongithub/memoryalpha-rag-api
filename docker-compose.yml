version: "3.9"

services:
  ollama:
    build:
      context: ./ollama
      dockerfile: ollama.Dockerfile
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - odn
    env_file:
      - .env
      
  lcars:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - ollama
    volumes:
      - .:/workspace/memoryalpha-rag-api
      - model_cache:/root/.cache
    ports:
      - "${API_PORT:-8000}:${APP_PORT:-8000}"  # host API_PORT -> container APP_PORT
    networks:
      - odn
    env_file:
      - .env
    entrypoint: ["./wait-for-ollama.sh", "uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "${APP_PORT:-8000}", "--log-level", "info"]

volumes:
  ollama_data:
  model_cache:

networks:
  odn:
    driver: bridge